/* var: declares a  variable, optionally initializaing it to a value. */
/* let: declares a block-scoped, local variable, optionally initializing it to a value. */
/* const: declares a block-scoped, read-only named constant. */

